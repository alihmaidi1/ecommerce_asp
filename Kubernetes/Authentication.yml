apiVersion: apps/v1
kind: Deployment

metadata:
  name: Authentication

spec:

  replicas: 1
  selector:
    matchLabels:
      app: Authentication
  template:
    metadata:
      labels:
        app: Authentication
    spec:
      containers:
      - name: AuthenticationService
        image: alihmaidi/ecommerce_authentication

